import { VehicleBookingsearchPipe } from './vehicle-bookingsearch.pipe';

describe('VehicleBookingsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new VehicleBookingsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
