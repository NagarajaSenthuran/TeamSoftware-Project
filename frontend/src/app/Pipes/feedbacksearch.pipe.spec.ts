import { FeedbacksearchPipe } from './feedbacksearch.pipe';

describe('FeedbacksearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FeedbacksearchPipe();
    expect(pipe).toBeTruthy();
  });
});
