import { VehiclesearchPipe } from './vehiclesearch.pipe';

describe('VehiclesearchPipe', () => {
  it('create an instance', () => {
    const pipe = new VehiclesearchPipe();
    expect(pipe).toBeTruthy();
  });
});
